# Only set this if deploying frontend SEPARATELY from the backend (e.g. on Netlify).
# Leave unset for unified deployment where FastAPI serves the frontend.
# Must be set at BUILD TIME (Vite compiles this into the bundle).
# Example: VITE_API_URL=https://squash-elo-production.up.railway.app
VITE_API_URL=
